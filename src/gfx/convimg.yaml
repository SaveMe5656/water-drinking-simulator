palettes:
  - name: wds
    fixed-entries:
      - image: palette.png

converts:
  - name: compressed
    palette: wds
    compress: zx7
    width-and-height: false
    images:
      - waterd2.png
      - waterd33.png

outputs:
  - type: c
    include-file: gfx.h
    palettes:
      - wds
    converts:
      - compressed
